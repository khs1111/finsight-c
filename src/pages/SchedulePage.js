/**
 * (기존) 일정/뉴스레터 자리 페이지
 * - 현재는 뉴스레터 기능이 /newsletter 경로로 분리됨
 * - 필요 시 일정/캘린더 전용 화면으로 재활용 예정
 */
import React from 'react';

export default function SchedulePage() {
  return (
    <div style={{padding:'88px 20px 120px'}}>뉴스레터 페이지 (준비중)</div>
  );
}
